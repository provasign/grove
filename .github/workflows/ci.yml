name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"
  GO_VERSION: "1.26.3"
jobs:
  test:
    name: Test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-go@v6
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true
      - run: go vet ./...
      - run: go test ./... -count=1 -short -timeout 120s
      - run: go build ./...

  verify-self:
    # Dogfood: report what prism verify finds on this PR's own diff.
    # Non-blocking. Grove does not depend on prism (prism depends on grove),
    # so prism is installed at a pin in its own module context.
    name: verify (self, reporting)
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - uses: actions/setup-go@v6
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true
      - name: install prism (pinned)
        run: go install github.com/provasign/prism/cmd/prism@v0.30.0
      - name: verify diff vs base
        continue-on-error: true
        run: |
          prism index . >/dev/null 2>&1 || true
          echo "## prism verify — this PR's diff vs ${{ github.base_ref }}" >> "$GITHUB_STEP_SUMMARY"
          prism verify . --base "origin/${{ github.base_ref }}" 2>&1 | tee -a "$GITHUB_STEP_SUMMARY"
